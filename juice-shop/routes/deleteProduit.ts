/* import { type Request, type Response } from 'express'

export function deleteProduit (req: Request, res: Response) {
  const id = req.params.id
  return res.status(200).json({ message: `Produit ${id} supprimé (simulation)` })
} */
